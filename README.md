# offline llm switcher

this is a small python app for switching between local llms.  
all models must be downloaded manually (gguf/bin format) and dropped into the `/models` folder.

### how to run
1. make sure you have python 3.10+
2. install deps: