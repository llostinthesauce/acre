# Requirements for NVIDIA Jetson devices
# Note: PyTorch must be installed separately from NVIDIA's Jetson wheels
# See JETSON_SETUP.md for installation instructions

# Base dependencies
customtkinter>=5.2.0
darkdetect>=0.8.0
pillow>=10.2.0
cryptography>=42.0.0
send2trash>=1.8.3
protobuf>=3.20,<6
soundfile>=0.12
pymupdf>=1.24

# llama-cpp-python (install with CUDA support: CMAKE_ARGS="-DLLAMA_CUBLAS=on" pip install llama-cpp-python)
# llama-cpp-python>=0.3.16

# Transformers and related (only if PyTorch is installed)
# transformers>=4.52.4
# diffusers>=0.25
# safetensors>=0.4
# huggingface_hub>=0.23
# accelerate>=0.31

# Note: torch and torchvision should be installed from NVIDIA's Jetson wheels, not from PyPI

